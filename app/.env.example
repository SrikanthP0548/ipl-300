# Base URL of the deployed Worker API. Defaults to http://localhost:8787 for local dev
# (see worker/README or `npm run dev` in ../worker). Set this to the real
# *.workers.dev (or custom domain) URL before building for production.
VITE_API_BASE_URL=http://localhost:8787
